apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo
  labels:
    app: rollouts-demo
spec:
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: rollouts-demo
