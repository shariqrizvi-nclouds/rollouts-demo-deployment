---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: 0.0.0.0/0
  labels:
    app: rollouts-demo
spec:
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: rollouts-demo
  type: LoadBalancer
